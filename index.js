function Reservar(onClick) {
    var reservationform = document.getElementById("Reservar").value;
    var reservation_list = document.getElementById("reservation_list");
  
    reservationList.innerHTML + "<" + reservationform + ">";
  }
  